# Handoff — RUN-0001（最终版）

Updated at: P6 complete · run closed · v0.1 tagged

## 已完成 Phase
- P0 Baseline（SD-1/SD-2 差异、环境探测）→ P1 Planning（ADR-0002/0003/0004，ISS-001/002）→ P2 Parallel（5 个 PR、3 个真实 sub-agent pod、mentoring/lookup/ambient）→ P3 Intermediate Gate（PASSED + 4 项真实缺陷修复）→ P4 Rework（QA 探针：离屏截图、俯视相机）→ P5 Final Gate（PASSED，v0.1）→ P6 R-001 + Wave Review。

## 当前 branch / tag
- main @ ef1e428+ · tag `v0.1`（未 push —— push 留给 Owner）

## 未合并 PR / branch
- 无（5 个 PR 全部 merge，worktree 已清理）。

## Open blocker
- 无。遗留 deferral（非阻塞）：
  1. 人肉手感验收 B1-B4 / C3 察觉率 / D3 —— **等 Owner 玩一次**（docs/qa/M0-smoke.md 待填）。
  2. Code Evolution View + 完整 Role Lens —— OW1。
  3. SD-1 的 🎓 转让待 Owner 追认。

## 关键决策摘要
- ADR-0002 raylib（Autonomous，调用面收敛）；ADR-0003 OW0 静态 Web + generator（投影只在 generator 侧）；ADR-0004 事件模型 v1.1（run/mentor/learn/ambient 四族 + ambient 四硬约束）。
- M1 方向候选 5 项已列 R-001 §三，**不做选型**，等 Owner 挑 1–2 个。

## 下一步 3 个动作（Owner）
1. 玩 `build/Release/mirage_m0.exe`（正常模式），填 docs/qa/M0-smoke.md 的人肉行——你对"察觉率"的判断决定 Kai 的 M1 优先级。
2. `git push origin main --tags`（本 run 未 push）。
3. 读 R-001 §三，挑 M1 方向；下次会话说「以 <成员> 身份开 M1 的 RFC」即可。
