# Handoff — RUN-0001

Updated at: P1 complete (logical)

## 已完成 Phase
- P0 Baseline：环境探测、仓库全量扫描、SD-1/SD-2 差异记录、Run Card。
- P1 Planning：RFC-0002/ADR-0002（raylib 5.5，Autonomous Project Decision，调用面收敛 main.cpp+scene.cpp）；RFC-0003/ADR-0003（OW0 = 静态 Web UI + Python generator）；RFC-0004/ADR-0004（事件模型 v1.1：run/phase/logical_seq + mentor/learn/ambient 族 + ambient 四硬约束）；ISS-001 done（工具链 spike exit 0 + CMake 骨架 + build.md + Learning Hook）；ISS-002 done；ISS-011~014（OW0 wave）建立；事件补记 seq 34–70（gap 54，容忍）。

## 当前 branch
- main @ 66992df+

## 未合并 PR / branch
- feature/m0-worldgraph（ver/vic sub-agent，worktree .worktrees/wt-worldgraph）
- feature/ow0-obslog（mora/milo sub-agent，worktree .worktrees/wt-obslog）
- feature/m0-app-shell（Vera，主 worktree）

## Open blocker
- 无。

## 关键决策摘要
- raylib 调用面只允许出现在 `src/main.cpp` + `src/scene.cpp`（ADR-0002，Rin 的条件）。
- OW0 投影只在 generator 侧计算，前端只渲染（ADR-0003，Ash 的条件）。
- fixture 默认关闭，Gate 视图永不显示 fixture（ADR-0003，Dot 的条件）。
- ambient 四硬约束不可协商（ADR-0004）。
- 事件日志仅 Orchestrator 写；`obslog check` 对未知 type 报 warning。

## 下一步 3 个动作
1. 等待/合入两个 sub-agent 分支，Vera 完成 app shell（feature/m0-app-shell）。
2. Rue（模拟）做 scene 投影分支；合入后启动 Nell（player/mutation/exit/smoke）。
3. Milo 合入后，Mora 做 ISS-011/012（view generator + web UI）。
