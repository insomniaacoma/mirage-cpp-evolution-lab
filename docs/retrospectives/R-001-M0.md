# R-001 · Milestone 0 Retrospective

- Date: 2026-09-06（logical: P6）· Chair: Arden · 全员意见原样保留
- 对照物：`docs/milestones/M0.md` 的疼痛点预测 P1–P5（Run Card 继承）

## 一、预测对账：哪些真的疼了？

| # | 预测 | 判定 | 证据 |
|---|---|---|---|
| P1 | main.cpp god file（Vera） | **疼了，轻度** | main.cpp 吸收了 demo 世界数据、输入接线、相机数学、HUD、smoke driver。尚可忍（~450 行）；第二个消费者出现时拆，走 RFC |
| P2 | 全量重建不可持续（Rin） | **未疼（计时中）** | 36 盒、拓扑只变 2 次。version-guard 设计足够。Mirage 频率才是触发器——预测本身"提前"，诚实记为未发生 |
| P3 | mutation 硬编码无事件概念（Nico） | **轻度成立** | MutationSpec 结构自然长出来了（触发数据与触发逻辑分离），单触发器无压力。第二个触发器类型出现时才是真考试 |
| P4 | 视觉 cue 察觉率存疑（Kai） | **未决（真悬置）** | 无玩家无法测。Kai：这是我入场的时机信号——audio module 的出生条件在成熟 |
| P5 | 手写 JSONL 手滑（Mora） | **疼了，已部分偿还** | Orchestrator 真实手滑：heredoc 截断产生重复行+gap（seq 54）、批量工具踩坑。obslog 落地后 check 立即兜住（0 error, 1 warning 如实报告）。工具的预言自我实现 |

**未被预测的最大疼痛：Windows/工具链环境摩擦。** C4819 代码页、DWM 节流、隐藏窗口无 framebuffer、CRLF、代理网络——消耗的真实时间超过任何架构问题。教训：**平台怪癖值得占一个预测槽位**（进 AGENTS.md 候选条款）。

**未被预测的成功：跨分支 API 契约。** scene 对着 Vic 的头文件契约一次性编译通过；失效矩阵纪律在 Nell 的接线里被正确遵守。契约先行 + 借出规则注释 = 低成本协调机制，保留。

## 二、哪些设计暂时仍然足够（明确不改，防无病呻吟）

- WorldGraph：int version + append-only 不变量 + 借出规则文档化（vic-vera mentoring 落地结论）
- `src/` 扁平 + 单 CMake target（P0 原则的持续胜利）
- InputFrame 接缝：真人/脚本输入同路径——DEC-003 被证明是对的
- obslog stdlib 路线 + 静态 Web UI（OW0 零依赖交付了七个视图）
- fixture 隔离 + Show Fixture 默认关（Dot 的条件在 UI 验证中被证明必要）

## 三、下一阶段真正值得增加什么（只定方向，不做选型）

1. **Audio module 的出生条件接近成熟**（Kai）：察觉率未测 + "音乐 phrase 驱动 mutation"是 Nico 憋了一整个 milestone 的玩法假说。M1 若做，走 RFC；realtime 边界红线从第一行代码就生效。
2. **Mirage 频率才是照妖镜**（Rin）：M1 若引入频繁局部变化，version() 升级路径（per-room stamp）会被真实压力逼出来——到那天再升级，那天之前忍住。
3. **内容数据外置**（Nico）：只在第二个关卡布局出现时做；DemoWorld 接线模式目前够用。
4. **main.cpp 拆分**（Vera）：app/ 与 game/ 的边界等第二个消费者出现，拆分本身就是一次 RFC/ADR 教学。
5. **OW1 自动化采集**（Mora/Milo）：commit.recorded / metrics 目前手工，git hook 与构建脚本能消除最大的记录摩擦。

## 四、Owner 意见（单独成节，待 Owner 补充确认）

- 交接给 Owner 的验收项：跑一次真人模式，回答 B1-B4 手感 + C3 察觉率 + D3 反馈是否清晰（docs/qa/M0-smoke.md 待填）。
- 🎓 学习路径：docs/learning/ 三份 Hook（cmake-toolchain / room-identity / append-only-validator），建议 Guess 模式重做 room-identity 练习。
- 待 Owner 裁决：SD-1 的🎓转让是否追认；M1 方向从上面五项中挑 1–2 个。

## 五、Arden 的守纪检查

- 无"为了先进而重写未疼代码"的提案 ✓
- 分歧保留：Rin（学习价值 vs raylib）、Dee（0 warning 任务书矛盾）、Nell（squeeze 叙事归属）均未被和稀泥 ✓
- 所有 Review 的 Request Changes 都产生了真实修改 ✓
